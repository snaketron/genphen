export(
  runGenphen,
  runDiagnostics,
  runPhyloBiasCheck
)

# Import packages listed as Imports or Depends
import(
  Rcpp,
  methods,
  stats,
  graphics,
  e1071,
  Biostrings,
  rstan,
  rstantools,
  ranger,
  parallel,
  foreach,
  doParallel
)
importFrom("graphics", "hist")
importFrom("stats", "bw.nrd0")
#exportPattern("^[[:alpha:]]+")
useDynLib(genphen, .registration = TRUE)
