# Export all names
export(
  runGenphen
)

# Import packages listed as Imports or Depends
import(
  e1071,
  ggplot2,
  effsize,
  Biostrings,
  rstan,
  boot
)

importFrom("randomForest", randomForest)

#exportPattern("^[[:alpha:]]+")
